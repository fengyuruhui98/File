# 2023-03-23-change

1. 钱箱
```c#
case DSString.CashBoxTypeOp:
    kvsl.Add(new KeyValuePair<string, string>("FF", "所有钱箱"));
    kvsl.Add(new KeyValuePair<string, string>("00", "纸币废钞箱"));
    kvsl.Add(new KeyValuePair<string, string>("01", "1元钞箱"));
    kvsl.Add(new KeyValuePair<string, string>("02", "5元钞箱"));
    kvsl.Add(new KeyValuePair<string, string>("04", "硬币备用箱A"));
    kvsl.Add(new KeyValuePair<string, string>("05", "硬币备用箱B"));
    kvsl.Add(new KeyValuePair<string, string>("06", "硬币回收箱"));
    break;
```
2. RP.0105.Income.Comsume.cs
line 105
```c#
string strSQL = string.Format(@" SELECT 
{0},
{1},
station_nid,
{2},
trim({3}),
sum(nvl(out_count,0)),
sum(nvl(out_amount,0)),
sum(nvl(card_pay_count,0)),
sum(nvl(card_pay_amount,0))
FROM TBL_BATCH_TRANS_SETTLE_HIS batchtable
where ticket_category IN ('1001','1002','1003') ",
DataFactoryRP.LineQuery,
DataFactoryRP.StationQuery, 
DataFactoryRP.TicketCategoryQuery, 
DataFactoryRP.TicketTypeQuery);
```